import Placeholder from '@/ui/Placeholder'

export default function Logger() {
  return (
    <Placeholder
      title="Workout"
      back="/train"
      description="Log load, reps and RIR per set with a rest timer."
    />
  )
}
