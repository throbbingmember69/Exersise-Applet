import Placeholder from '@/ui/Placeholder'

export default function Settings() {
  return (
    <Placeholder
      title="Settings"
      back="/more"
      description="Every rule threshold, tagged Evidence or Heuristic, and editable."
    />
  )
}
